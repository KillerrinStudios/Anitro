<UserControl
    x:Class="Anitro.Controls.ProgressIndicator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Anitro.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="parent">

    <Grid x:Name="LayoutRoot" DataContext="{Binding ElementName=parent}">
        <StackPanel Orientation="Horizontal">
            <ProgressRing Width="50" Height="50" IsActive="{Binding IsRingActive}"/>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock HorizontalAlignment="Center" Visibility="{Binding PercentageVisibility}">
                    <Run Text="{Binding PercentageCompleted}"/>
                    <Run Text="%"/>
                </TextBlock>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
