<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Anitro"
    xmlns:local2="using:Anitro.Controls">

    <Style TargetType="local2:UserAccountButton" xmlns:local2="using:Anitro.Controls">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:UserAccountButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{TemplateBinding Header}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <Image Source="{TemplateBinding Avatar}"/>
                                <TextBlock Text="{TemplateBinding Username}" Margin="5,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
