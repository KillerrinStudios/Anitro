<Page
	x:Class="Anitro.MainPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Anitro"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource AnitroBlue}"
    Loaded="MainPage_Loaded">

    <Grid x:Name="MainGrid" Background="{ThemeResource AnitroBlue}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <MediaElement x:Name="player" Visibility="Collapsed"/>

        <Image Source="Assets/SplashScreen.scale-100.png" Margin="0,0,0,20" Height="300" Width="620"
               VerticalAlignment="Center" HorizontalAlignment="Center" />

        <TextBlock x:Name="debugTextBlock" Text=""
                   Width="Auto" Height="Auto" Margin="10,10,10,10"
                   HorizontalAlignment="Center" TextWrapping="Wrap"/>

        <ProgressRing x:Name="ApplicationProgressBar" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,50" Height="200" Width="200" IsActive="True" Background="#00000000"/>
    </Grid>
</Page>