<Page x:ConnectionId='1'
	x:Class="Anitro.LibraryPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Anitro"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                               >

	<Page.BottomAppBar>
		<CommandBar>
			<CommandBar.PrimaryCommands>
				<AppBarButton x:ConnectionId='2' x:Uid="Search" Icon="Find" Label="search"                        />
				<AppBarButton x:ConnectionId='3' x:Uid="Refresh" Icon="Refresh" Label="refresh"                         />
			</CommandBar.PrimaryCommands>
			<CommandBar.SecondaryCommands>
				<AppBarButton x:ConnectionId='4' x:Uid="about" Icon="Tag" Label="about"                       />
                <AppBarButton x:ConnectionId='5' x:Uid="clearRecentButton" Icon="ClearSelection" Label="clear recent"                             />
            </CommandBar.SecondaryCommands>
		</CommandBar>
	</Page.BottomAppBar>

	<Grid>
		<ProgressRing x:Name="ApplicationProgressBar" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,50,0" Height="32" Width="32" IsActive="False"/>

        <Pivot x:Name="libraryPivot" Title="Library">
            <PivotItem x:Name="recent_PivotItem" Header="Recent">
                <GridView x:ConnectionId='12' x:Name="recent_GridView" Margin="0,0,0,0" ScrollViewer.VerticalScrollMode="Enabled" IsItemClickEnabled="True"
							                                     
							  
							  ItemTemplate="{StaticResource RecentDataTemplate}"
							  ItemsPanel="{StaticResource RecentItemsPanelTemplate}"
							  ItemContainerStyle="{StaticResource RecentItemContainerStyle}"/>
            </PivotItem>
            <PivotItem x:Name="currently_PivotItem" Header="Currently Watching">
                <Grid>
                    <ListBox x:ConnectionId='11' x:Name="currentlyWatching_ListBox" Margin="0,0,0,0" Background="Transparent" FontSize="26" 
								                        
								 ItemTemplate="{StaticResource AnimeLibraryTemplate}"/>
                </Grid>
            </PivotItem>
            <PivotItem x:Name="planTo_PivotItem" Header="Plan To Watch">
                <Grid>
                    <ListBox x:ConnectionId='10' x:Name="planToWatch_ListBox" Margin="0,0,0,0" Background="Transparent" FontSize="26" 
								                        
								 ItemTemplate="{StaticResource AnimeLibraryTemplate}"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="Completed">
                <Grid>
                    <ListBox x:ConnectionId='9' x:Name="completed_ListBox" Margin="0,0,0,0" Background="Transparent" FontSize="26" 
								                        
								 ItemTemplate="{StaticResource AnimeLibraryTemplate}"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="On Hold">
                <Grid>
                    <ListBox x:ConnectionId='8' x:Name="onHold_ListBox" Margin="0,0,0,0" Background="Transparent" FontSize="26" 
								                        
								 ItemTemplate="{StaticResource AnimeLibraryTemplate}"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="Dropped">
                <Grid>
                    <ListBox x:ConnectionId='7' x:Name="dropped_ListBox" Margin="0,0,0,0" Background="Transparent" FontSize="26" 
								                        
								 ItemTemplate="{StaticResource AnimeLibraryTemplate}"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="Favourites">
                <GridView x:ConnectionId='6' x:Name="favourites_GridView" Margin="0,0,0,0" ScrollViewer.VerticalScrollMode="Enabled" IsItemClickEnabled="True"
							                                         
							  
							  ItemTemplate="{StaticResource AnimeFavouritesDataTemplate}"
							  ItemsPanel="{StaticResource AnimeFavouritesItemsPanelTemplate}"
							  ItemContainerStyle="{StaticResource AnimeFavouritesItemContainerStyle}"/>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

