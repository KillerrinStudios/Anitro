<ResourceDictionary xmlns:Controls="using:Callisto.Controls" 
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Anitro.Themes">

	<DataTemplate x:Key="AnimeLibraryTemplate">
		<StackPanel Orientation="Horizontal" Margin="0,-25,0,-40" VerticalAlignment="Top" Width="400" Height="200">
			<Image Width="100" Height="200" Margin="0,-10,0,0">
				<Image.Source>
                    <BitmapImage UriSource="{Binding Anime.CoverImageUrlString}" CreateOptions="None" />
				</Image.Source>
			</Image>

			<StackPanel Orientation="Vertical" Margin="10,25,0,0" VerticalAlignment="Top">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="{Binding Anime.RomanjiTitle}" Margin="0,0,0,0" Width="600" FontSize="20" Foreground="{StaticResource TitleBrush}"/>
					    <TextBlock Text="{Binding Anime.EnglishTitle}" Margin="0,-5,0,0" Width="600" FontSize="20" Foreground="{StaticResource ContentBrush}"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,5,0,0">
                        <TextBlock Margin="0,0,0,0" Width="auto" FontSize="18" Foreground="{StaticResource ContentBrush}">
													<Run Text="rating: " />
													<Run Text="{Binding Rating}" />
													<Run Text="/" />
													<Run Text="5" />
                        </TextBlock>

                        <TextBlock Margin="0,0,0,0" Width="auto" FontSize="18" Foreground="{StaticResource ContentBrush}">
													<Run Text="watched: " />
													<Run Text="{Binding EpisodesWatched}" />
													<Run Text="/" />
													<Run Text="{Binding Anime.EpisodeCount}" />
                        </TextBlock>
                    </StackPanel>
                </Grid>
			</StackPanel>
		</StackPanel>
	</DataTemplate>
</ResourceDictionary>

